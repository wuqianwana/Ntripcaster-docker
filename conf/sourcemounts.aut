# 源挂载身份验证文件。
# 在此处指定哪些组对哪些挂载点具有源访问权限，
# 每个挂载点一行。
#
# 语法：<MOUNTPOINT>:<GROUP1>,<GROUP2>,...,<GROUPn>
#
# <MOUNTPOINT>：挂载点的名称。必须以斜杠开头。
# 如果使用特殊关键字“all”（不带斜杠）作为挂载点，
# 该行指定的组对所有挂载点拥有完全访问权限，
# 所有其他行将被忽略。
# 如果使用特殊关键字“rest”（不带斜杠），
# 该行指定的组将对没有限制的挂载点拥有源访问权限。
# <GROUPi>：对 <MOUNTPOINT> 具有源访问权限的组的名称。

all:uploader,gpsclient
