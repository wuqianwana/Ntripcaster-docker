# 客户端挂载身份验证文件。
# 在此处指定哪些组对哪些挂载点具有客户端访问权限，
# 每个挂载点占一行。
#
# 语法：<MOUNTPOINT>:<GROUP1>,<GROUP2>,...,<GROUPn>
#
# <MOUNTPOINT>：挂载点的名称。必须以斜杠开头。
# 应始终指定挂载点“/admin”和“/oper”，
# 否则，每个人都可以通过 Web 界面完全访问服务器。
# 如果使用特殊关键字“all”（不带斜杠）作为挂载点，
# 则该行中指定的组对所有挂载点拥有完全访问权限，所有其他行都将被忽略。
# 如果使用特殊关键字“rest”（不带斜杠），则该行中指定的组将对没有限制的挂载点拥有客户端访问权限。
# <GROUPi>：对 <MOUNTPOINT> 具有客户端访问权限的组的名称。
/SHBDS:gpsclient

/admin:FirstAdmin
/oper:FirstAdmin
